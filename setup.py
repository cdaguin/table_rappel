#!/usr/bin/env python

from distutils.core import setup

setup(name='rappel',
      version='0.1',
      description='Apprentissage des tables de rappels',
      author='Christophe Daguin',
      author_email='cdaguin@free.fr',
      url='no url so far',
      packages=['distutils', 'distutils.command'],
     )
